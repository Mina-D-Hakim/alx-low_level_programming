#include <stdio.h>
#include <unistd.h>
/**
 * main -  Print base 16 numbers
 * Return: Always 0 (success)
 */
int main(void)
{
	char c;
	char ch = '0';

	while (ch <= '9')
	{
		putchar(chd);
		ch++;
	}
	for (c = 'a'; c <= 'f'; c++)
	{
		putchar(c);
	}
	putchar('\n');
	return (0);
}
